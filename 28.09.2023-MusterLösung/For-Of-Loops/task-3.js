// Verwende eine For-of-Schleife, um die Summe der Elemente eines Arrays zu berechnen.

const myArray = [1, 2, 3, 4, 5];
let sum = 0;
for (const element of myArray) {
    sum += element;
}
console.log(sum);
